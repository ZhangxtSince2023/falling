{
  "name": "falling",
  "version": "0.0.1",
  "description": "一个简单的手机休闲游戏，玩家控制小球在不断下落的过程中躲避触碰屏幕上边界。",
  "main": "src/game-scene.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf www && mkdir -p www/src && cp index.html www/ && cp src/*.js www/src/ && cp -r assets www/",
    "build:ios": "npm run build && npx cap sync ios",
    "build:android": "npm run build && npx cap sync android",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android",
    "cap:run:ios": "npm run build:ios && npx cap run ios",
    "cap:run:android": "npm run build:android && npx cap run android",
    "dev": "python3 -m http.server 8000"
  },
  "keywords": [
    "game",
    "phaser",
    "mobile",
    "falling",
    "casual"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@capacitor/ios": "^7.4.4",
    "@capacitor/splash-screen": "^7.0.3",
    "@capacitor/status-bar": "^7.0.3",
    "playwright": "^1.56.1"
  },
  "dependencies": {
    "@capacitor/haptics": "^7.0.2",
    "axios": "^1.13.2",
    "form-data": "^4.0.5",
    "google-auth-library": "^10.5.0"
  }
}
